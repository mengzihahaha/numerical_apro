# 第一章 习题解答

---

## 1. 已知近似数 $x^*$ 的相对误差限为 0.05%，问它至少有几位有效数字？

### 解


由题意：

$$
\frac{|x^* - x|}{|x^*|} \leq 0.05\% = 0.0005 = 5 \times 10^{-4}
$$

要确定至少有几位有效数字，考虑最不利的情况 $a_1 = 9$（因为 $\frac{1}{2a_1}$ 最小），则需要：

$$
\frac{1}{2 \times 9} \times 10^{-(n-1)} \leq 5 \times 10^{-4}
$$

$$
\frac{10^{-(n-1)}}{18} \leq 5 \times 10^{-4}
$$

$$
10^{-(n-1)} \leq 90 \times 10^{-4} = 9 \times 10^{-3}
$$

两边取对数：

$$
-(n-1) \leq \lg(9 \times 10^{-3}) = \lg 9 + \lg 10^{-3} \approx 0.9542 - 3 = -2.0458
$$

$$
n - 1 \geq 2.0458 \implies n \geq 3.0458
$$

因此 $n \geq 4$，即 **至少有 3 位有效数字**。

> 验证：若 $n=3$，取 $a_1=1$（最有利的情况），$\frac{1}{2 \times 1} \times 10^{-2} = 0.005$，不满足；取 $a_1=9$，$\frac{1}{2 \times 9} \times 10^{-2} \approx 0.00556$，也不满足。实际上，对 3 位有效数字，最大的相对误差限为 $\frac{1}{2a_1} \times 10^{-2}$，当 $a_1=1$ 时为 0.005，即 0.5%，而题目给出的是 0.05%，更严格。因此 **$n \geq 3$ 可以满足**（因为 $\frac{1}{2 \times 1} \times 10^{-2} = 0.005 > 0.0005$，但当 $a_1 \geq 1$ 时总有 $\frac{1}{2a_1} \times 10^{-2} \leq 0.005$，而 0.0005 比 0.005 小 10 倍，所以需要 $n \geq 3$）。

**答案：至少有 3 位有效数字。**

---

## 2. 说明当 $N$ 足够大时，应该如何计算 $\int_N^{N+1} \frac{1}{x^2+1} dx$。

### 解

直接用基本积分公式：

$$
\int \frac{1}{x^2+1} dx = \arctan x + C
$$

所以：

$$
\int_N^{N+1} \frac{1}{x^2+1} dx = \arctan(N+1) - \arctan N
$$

当 $N$ 很大时，$\arctan(N+1)$ 和 $\arctan N$ 都非常接近 $\frac{\pi}{2}$，两者相减会出现**严重的有效数字丢失**（两个接近相等的数相减）。

### 改进方法

利用**反三角函数恒等式**：

$$
\arctan a - \arctan b = \arctan\left( \frac{a - b}{1 + ab} \right)
$$

取 $a = N+1,\ b = N$：

$$
\arctan(N+1) - \arctan N = \arctan\left( \frac{(N+1) - N}{1 + (N+1)N} \right) = \arctan\left( \frac{1}{N^2 + N + 1} \right)
$$

当 $N$ 足够大时，$\frac{1}{N^2 + N + 1}$ 很小，利用 $\arctan x \approx x$（$x$ 很小时）：

$$
\arctan\left( \frac{1}{N^2 + N + 1} \right) \approx \frac{1}{N^2 + N + 1}
$$

或者更精确地，使用泰勒展开：

$$
\arctan x = x - \frac{x^3}{3} + \frac{x^5}{5} - \cdots
$$

其中 $x = \frac{1}{N^2 + N + 1}$。

### 综上所述

**不应直接计算 $\arctan(N+1) - \arctan N$，而应先利用恒等式转换为 $\arctan\left(\frac{1}{N^2+N+1}\right)$，再用泰勒展开近似计算。**

---

## 3. 已知 $\sin 1^\circ \approx 0.0175$，求 $\cos 2^\circ$。

### 解

利用**二倍角公式**：

$$
\cos 2\theta = 1 - 2\sin^2 \theta
$$

取 $\theta = 1^\circ$，则：

$$
\cos 2^\circ = 1 - 2\sin^2 1^\circ
$$

代入 $\sin 1^\circ \approx 0.0175$：

$$
\sin^2 1^\circ \approx (0.0175)^2 = 0.00030625
$$

$$
\cos 2^\circ \approx 1 - 2 \times 0.00030625 = 1 - 0.0006125 = 0.9993875
$$

### 验证

精确值 $\cos 2^\circ \approx 0.99939083$，误差约为 $3.3 \times 10^{-6}$，主要来自 $\sin 1^\circ$ 的近似值（精确 $\sin 1^\circ \approx 0.017452406$）。

**答案：$\cos 2^\circ \approx 0.9994$（保留 4 位有效数字）**

---

## 4. 假如你有一个 4 位数的平方根表，如何计算方程 $x^2 + 100x - 1 = 0$ 的两个根？

### 解

对于一般二次方程 $ax^2 + bx + c = 0$，求根公式为：

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

对于方程 $x^2 + 100x - 1 = 0$，有 $a=1,\ b=100,\ c=-1$：

$$
x = \frac{-100 \pm \sqrt{10000 + 4}}{2} = \frac{-100 \pm \sqrt{10004}}{2}
$$

### 问题分析

$\sqrt{10004}$ 可以表示为 $\sqrt{10000 \times 1.0004} = 100\sqrt{1.0004}$，但平方根表只给 4 位数（即 1 到 9999 之间的整数）。

### 利用平方根近似公式

对于 $\sqrt{a^2 \pm b} \approx a \pm \frac{b}{2a}$（$b \ll a^2$），这里 $10004 = 100^2 + 4$，所以：

$$
\sqrt{10004} = \sqrt{100^2 + 4} \approx 100 + \frac{4}{2 \times 100} = 100 + 0.02 = 100.02
$$

或者更精确地用 $\sqrt{a^2 + b} \approx a + \frac{b}{2a} - \frac{b^2}{8a^3}$：

$$
\sqrt{10004} \approx 100 + \frac{4}{200} - \frac{16}{8 \times 10^6} = 100.02 - 0.000002 = 100.019998
$$

精确值 $\sqrt{10004} \approx 100.019998$，非常接近。

### 求两个根

$$
x_1 = \frac{-100 + \sqrt{10004}}{2} \approx \frac{-100 + 100.02}{2} = \frac{0.02}{2} = 0.01
$$

$$
x_2 = \frac{-100 - \sqrt{10004}}{2} \approx \frac{-100 - 100.02}{2} = \frac{-200.02}{2} = -100.01
$$

**答案：两个根分别为 $x_1 \approx 0.01$ 和 $x_2 \approx -100.01$。**

---

## 5. 利用余弦函数的泰勒展开，计算 $\cos 1$ 的值，保留 4 位有效数字。

### 解

余弦函数的**麦克劳林展开**（在 $x=0$ 处的泰勒展开）：

$$
\cos x = 1 - \frac{x^2}{2!} + \frac{x^4}{4!} - \frac{x^6}{6!} + \cdots + (-1)^n \frac{x^{2n}}{(2n)!} + \cdots
$$

取 $x = 1$（弧度），级数为：

$$
\cos 1 = 1 - \frac{1}{2} + \frac{1}{24} - \frac{1}{720} + \frac{1}{40320} - \cdots
$$

### 逐项计算

| 项数 | 计算 | 值 | 累计和 |
|:---:|------|:----:|:------:|
| $n=0$ | $1$ | $1$ | $1$ |
| $n=1$ | $-\frac{1}{2}$ | $-0.5$ | $0.5$ |
| $n=2$ | $+\frac{1}{24}$ | $0.041666\cdots$ | $0.541666\cdots$ |
| $n=3$ | $-\frac{1}{720}$ | $-0.001388\cdots$ | $0.540278\cdots$ |
| $n=4$ | $+\frac{1}{40320}$ | $0.0000248\cdots$ | $0.540302\cdots$ |

### 误差分析（交错级数）

这是一个**交错级数**，余项的绝对值小于被省略的第一项绝对值：

$$
|R_n| < \frac{1}{(2(n+1))!}
$$

取 $n=3$ 时，$|R_3| < \frac{1}{40320} \approx 2.48 \times 10^{-5}$，远小于 $5 \times 10^{-4}$（4 位有效数字的误差限），已经足够。

### 结果

$$
\cos 1 \approx 0.5403
$$

验证：精确值 $\cos 1 \approx 0.5403023059$，误差约为 $2.3 \times 10^{-7}$。

**答案：$\cos 1 \approx 0.5403$（保留 4 位有效数字）**

---

## 6. 利用近似公式 $\sqrt{a^2 \pm b} \approx a \pm \frac{b}{2a}$，计算 $\sqrt{7}$ 的值，保留 4 位有效数字。试着计算 $\sqrt[3]{7}$。

### 解

#### (1) 计算 $\sqrt{7}$

寻找最接近 7 的完全平方数：$a^2 = 4,\ 9$，选 $a=3$（因为 9 比 7 大 2，差较小）：

$$
\sqrt{7} = \sqrt{3^2 - 2}
$$

利用近似公式 $\sqrt{a^2 - b} \approx a - \frac{b}{2a}$，其中 $a=3,\ b=2$：

$$
\sqrt{7} \approx 3 - \frac{2}{2 \times 3} = 3 - \frac{1}{3} = 3 - 0.3333\cdots = 2.6667
$$

验证：精确值 $\sqrt{7} \approx 2.6458$，误差约为 0.0209。

如果用更精确的公式 $\sqrt{a^2 - b} \approx a - \frac{b}{2a} - \frac{b^2}{8a^3}$：

$$
\sqrt{7} \approx 3 - \frac{2}{6} - \frac{4}{8 \times 27} = 3 - 0.333333 - 0.018519 = 2.6481
$$

误差约为 0.0023，已经很接近。进一步用 $\sqrt{a^2 - b} \approx a - \frac{b}{2a} - \frac{b^2}{8a^3} - \frac{b^3}{16a^5}$：

$$
\sqrt{7} \approx 3 - \frac{2}{6} - \frac{4}{216} - \frac{8}{16 \times 243} \approx 2.6458
$$

误差约为 $3 \times 10^{-5}$。

**$\sqrt{7} \approx 2.646$（保留 4 位有效数字）**

---

#### (2) 计算 $\sqrt[3]{7}$

寻找最接近 7 的完全立方数：$a^3 = 8$（$2^3 = 8$），所以：

$$
\sqrt[3]{7} = \sqrt[3]{2^3 - 1}
$$

对于立方根，利用二项式展开 $(a^3 - b)^{1/3} = a \left(1 - \frac{b}{a^3}\right)^{1/3}$，再用广义二项式定理：

$$
(1 + x)^\alpha = 1 + \alpha x + \frac{\alpha(\alpha-1)}{2!}x^2 + \cdots
$$

取 $x = -\frac{1}{8}$，$\alpha = \frac{1}{3}$：

$$
\sqrt[3]{7} = 2 \left(1 - \frac{1}{8}\right)^{1/3} \approx 2 \left[1 + \frac{1}{3}\left(-\frac{1}{8}\right) + \frac{\frac{1}{3}\left(\frac{1}{3}-1\right)}{2}\left(-\frac{1}{8}\right)^2\right]
$$

$$
= 2 \left[1 - \frac{1}{24} + \frac{\frac{1}{3} \cdot \left(-\frac{2}{3}\right)}{2} \cdot \frac{1}{64}\right]
= 2 \left[1 - \frac{1}{24} - \frac{1}{9 \times 64}\right]
$$

$$
\approx 2 \left[1 - 0.0416667 - 0.0017361\right] \approx 2 \times 0.956597 \approx 1.9132
$$

或者可以用类似平方根的近似公式推导：对于 $\sqrt[3]{a^3 \pm b}$，利用 $(a \pm c)^3 \approx a^3 \pm 3a^2 c$，令 $(a \pm c)^3 \approx a^3 \pm b$，则 $c \approx \pm \frac{b}{3a^2}$，故：

$$
\sqrt[3]{a^3 \pm b} \approx a \pm \frac{b}{3a^2}
$$

取 $a=2,\ b=1$（注意是 $-$ 号）：

$$
\sqrt[3]{7} \approx 2 - \frac{1}{3 \times 4} = 2 - \frac{1}{12} = 2 - 0.083333\cdots = 1.9167
$$

这比上面用二项式展开的结果略差，但仍然是合理近似。

**$\sqrt[3]{7} \approx 1.913$（保留 4 位有效数字）**，精确值约为 1.91293。

---

## 7. (1) 计算 $a = (10 + \sqrt{99})^4$ 的整数部分；(2) 计算 $a = (10 + \sqrt{99})^4$，保留 20 位有效数字。

### 解

#### (1) 整数部分计算

注意到：

$$
(10 - \sqrt{99})(10 + \sqrt{99}) = 100 - 99 = 1
$$

因此 $\frac{1}{10 + \sqrt{99}} = 10 - \sqrt{99}$。

令 $x = 10 + \sqrt{99}$，$y = 10 - \sqrt{99}$，则 $xy = 1$。

计算 $x + y = 20$，$x - y = 2\sqrt{99}$。

计算 $x^4 + y^4$，利用完全平方公式：

$$
x^2 + y^2 = (x + y)^2 - 2xy = 20^2 - 2 \times 1 = 400 - 2 = 398
$$

$$
x^4 + y^4 = (x^2 + y^2)^2 - 2(xy)^2 = 398^2 - 2 \times 1^2 = 158404 - 2 = 158402
$$

现在分析 $y = 10 - \sqrt{99}$：

$$
\sqrt{99} \approx 9.9499, \quad y \approx 10 - 9.9499 = 0.0501
$$

所以 $0 < y < 1$，故 $0 < y^4 < 1$。

又因为 $x^4 + y^4 = 158402$，所以：

$$
x^4 = 158402 - y^4
$$

由于 $0 < y^4 < 1$，所以 $158401 < x^4 < 158402$。

**因此 $a = x^4$ 的整数部分为 $\boxed{158401}$。**

---

#### (2) 保留 20 位有效数字

$y = 10 - \sqrt{99}$，由于 $x^4 + y^4 = 158402$，所以：

$$
x^4 = 158402 - y^4
$$

计算 $y = 10 - \sqrt{99}$：

$\sqrt{99} = 3\sqrt{11} \approx 9.9498743710661995473447982100121\cdots$

$y \approx 0.0501256289338004526552017899879\cdots$

$y^2 \approx 0.002512578660322426\cdots$

$y^4 = (y^2)^2 \approx (0.002512578660322426)^2 \approx 0.00000631305955\cdots$

所以：

$$
x^4 = 158402 - 0.00000631305955\cdots \approx 158401.99999368694045\cdots
$$

精确计算：

$$
x^4 = (10 + \sqrt{99})^4
$$

展开：

$$
(10 + \sqrt{99})^2 = 100 + 20\sqrt{99} + 99 = 199 + 20\sqrt{99}
$$

$$
(10 + \sqrt{99})^4 = (199 + 20\sqrt{99})^2 = 199^2 + 2 \times 199 \times 20\sqrt{99} + (20\sqrt{99})^2
$$

$$
= 39601 + 7960\sqrt{99} + 400 \times 99 = 39601 + 7960\sqrt{99} + 39600
$$

$$
= 79201 + 7960\sqrt{99}
$$

计算 $\sqrt{99}$ 到足够精度：

$\sqrt{99} \approx 9.94987437106619954734479821001214536\cdots$

$7960 \times \sqrt{99} \approx 7960 \times 9.94987437106619955 \approx 79200.99999368694\cdots$

所以：

$$
a = 79201 + 7960\sqrt{99} \approx 158401.999993686940453
$$

**保留 20 位有效数字：$a \approx 158401.99999368694045$**（即 $79201 + 7960\sqrt{99}$）

